A script to remove unused runtimes
Usage:
  -l string
    	Remove runtimes with versions lower than this (default "0.0")
  -u string
    	Remove runtimes with versions higher than this (default "99.0")